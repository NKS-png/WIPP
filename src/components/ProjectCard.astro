---
import ImageViewer from './ImageViewer.astro';

const { project } = Astro.props;

const emojis = ['😀', '😃', '😄', '😁', '😆', '😅', '😂', '🤣', '😊', '😇', '🙂', '🙃', '😉', '😌', '😍', '🥰', '😘', '😗', '😙', '😚', '😋', '😛', '😝', '😜', '🤪', '🤨', '🧐', '🤓', '😎', '🤩', '🥳', '😏', '😒', '😞', '😔', '😟', '😕', '🙁', '☹️', '😣', '😖', '😫', '😩', '🥺', '😢', '😭', '😤', '😠', '😡', '🤬', '🤯', '😳', '🥵', '🥶', '😱', '😨', '😰', '😥', '😓', '🤗', '🤔', '🤭', '🤫', '🤥', '😶', '😐', '😑', '😬', '🙄', '😯', '😦', '😧', '😮', '😲', '🥱', '😴', '🤤', '😪', '😵', '🤐', '🥴', '🤢', '🤮', '🤧', '😷', '🤒', '🤕', '🤑', '🤠', '😈', '👿', '👹', '👺', '🤡', '💩', '👻', '💀', '☠️', '👽', '👾', '🤖', '🎃', '😺', '😸', '😹', '😻', '😼', '😽', '🙀', '😿', '😾'];

function getEmojiAvatar(username: string): string {
  // Add null safety check
  if (!username) username = 'user';
  const hash = String(username).split('').reduce((a, b) => a + b.charCodeAt(0), 0);
  const emoji = emojis[hash % emojis.length];
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><rect width="100" height="100" fill="#f3f4f6"/><text x="50" y="65" font-size="50" text-anchor="middle">${emoji}</text></svg>`;
  
  // Use btoa instead of Buffer for better serverless compatibility
  try {
    return `data:image/svg+xml;base64,${btoa(svg)}`;
  } catch (error) {
    // Fallback to URL encoding if btoa fails
    return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`;
  }
}

// Generate human context cues
function getHumanCue(project) {
  const cues = [
    "Still experimenting",
    "Feedback welcome", 
    "Work in progress",
    "Early stages",
    "Just started this",
    "Version 2",
    "Trying something new",
    "Almost there"
  ];
  
  // Use project ID to consistently assign the same cue - ensure project.id exists and is a string
  if (!project?.id) return cues[0];
  const hash = String(project.id).split('').reduce((a, b) => a + b.charCodeAt(0), 0);
  return cues[hash % cues.length];
}

// Calculate days since creation for time-based cues
function getDaysAgo(dateString) {
  const created = new Date(dateString);
  const now = new Date();
  const diffTime = Math.abs(now - created);
  const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
  return diffDays;
}

const daysAgo = getDaysAgo(project.created_at);
const humanCue = getHumanCue(project);
---

<a href={`/project/${project.id}`} class="group block relative bg-stone-100 dark:bg-stone-800 border border-stone-200 dark:border-stone-700 rounded-2xl overflow-hidden hover:shadow-lg transition-all duration-300 hover:-translate-y-1">

  <div class="aspect-[4/3] overflow-hidden bg-stone-200 dark:bg-stone-700 relative">
    {project.images && project.images.length > 0 ? (
      <ImageViewer images={project.images} title={project.title} className="h-full" />
    ) : project.image_url ? (
      <ImageViewer images={[project.image_url]} title={project.title} className="h-full" />
    ) : (
      <div class="w-full h-full flex items-center justify-center text-stone-400">
        <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
      </div>
    )}
    
    <!-- Human Context Cue -->
    <div class="absolute top-3 left-3">
      <span class="text-xs bg-stone-800/80 dark:bg-stone-200/80 text-stone-50 dark:text-stone-900 px-2 py-1 rounded-lg font-medium backdrop-blur-sm">
        {daysAgo <= 1 ? 'Just started' : daysAgo <= 7 ? daysAgo + ' days in' : humanCue}
      </span>
    </div>
    
    <div class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
  </div>

  <div class="p-5">
    <div class="flex items-center justify-between mb-2">
      <div class="text-xs font-medium text-stone-600 dark:text-stone-400 uppercase tracking-wide">
        {project.category || 'General'}
      </div>
      <div class="text-xs text-stone-500 dark:text-stone-400">
        {new Date(project.created_at).toLocaleDateString()}
      </div>
    </div>

    <h3 class="font-medium text-base sm:text-lg text-stone-800 dark:text-stone-200 line-clamp-2 group-hover:text-stone-600 dark:group-hover:text-stone-300 transition-colors mb-2">
      {project.title}
    </h3>
    
    <p class="text-sm text-stone-600 dark:text-stone-400 line-clamp-2 mb-4">
      {project.description}
    </p>

    <div class="flex items-center gap-3 pt-3 border-t border-stone-200 dark:border-stone-700">
      <div class="w-6 h-6 rounded-xl bg-stone-200 dark:bg-stone-700 overflow-hidden flex-shrink-0 border border-stone-300 dark:border-stone-600">
        <img src={project.profiles?.avatar_url || getEmojiAvatar(project.profiles?.username || 'user')} class="w-full h-full object-cover" />
      </div>
      <span class="text-sm font-normal text-stone-700 dark:text-stone-300 truncate">
        {project.profiles?.full_name || project.profiles?.username || 'Anonymous'}
      </span>
    </div>
  </div>
</a>
